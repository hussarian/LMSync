<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

  <mapper namespace="com.ict.lmsPermission.permission.Permission">
    <select id="selectAll" resultType="com.ict.demo.staff.StaffPermission">
        SELECT * FROM staff_permission where pmId = #{pmId}
    </select>
  </mapper>